import { CoefficientFilterPipe } from './coefficient-filter.pipe';

describe('CoefficientFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoefficientFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
